{
  "name": "AI CTA/CTD Optimization",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "181df836-8c89-47d2-b611-07cd556473f8",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "webhook-trigger",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 1.1,
      "position": [240, 300],
      "webhookId": "181df836-8c89-47d2-b611-07cd556473f8"
    },
    {
      "parameters": {
        "resource": "row",
        "operation": "getAll",
        "tableId": "notifications",
        "returnAll": false,
        "limit": 1000,
        "filterType": "manual",
        "matchMode": "all",
        "filters": {
          "conditions": [
            {
              "keyName": "id",
              "condition": "in",
              "keyValue": "={{ $json.body.notification_ids }}"
            }
          ]
        },
        "selectFields": "id, property_id, unit_id, property_name, unit_name, change_type, start_date, end_date, created_at",
        "sort": [
          {
            "keyName": "start_date",
            "order": "ascending"
          }
        ]
      },
      "id": "fetch-notifications",
      "name": "Fetch Notifications",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [460, 300],
      "credentials": {
        "supabaseApi": {
          "id": "YOUR_SUPABASE_CREDENTIAL_ID",
          "name": "Supabase account"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// Extract unique property and unit IDs\nconst notifications = $input.all();\nconst notifData = notifications.map(n => n.json);\n\nconst unitIds = [...new Set(notifData.map(n => n.unit_id))];\nconst propertyIds = [...new Set(notifData.map(n => n.property_id))];\n\nreturn [{\n  json: {\n    notifications: notifData,\n    unit_ids: unitIds,\n    property_ids: propertyIds\n  }\n}];"
      },
      "id": "extract-ids",
      "name": "Extract IDs",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [680, 300]
    },
    {
      "parameters": {
        "resource": "row",
        "operation": "getAll",
        "tableId": "reservations",
        "returnAll": false,
        "limit": 10000,
        "filterType": "manual",
        "matchMode": "all",
        "filters": {
          "conditions": [
            {
              "keyName": "unit_id",
              "condition": "in",
              "keyValue": "={{ $json.unit_ids }}"
            },
            {
              "keyName": "check_in",
              "condition": "gte",
              "keyValue": "={{ new Date(Date.now() - 180*24*60*60*1000).toISOString().split('T')[0] }}"
            }
          ]
        },
        "selectFields": "unit_id, check_in, check_out, status, created_at, nights, total_price",
        "sort": [
          {
            "keyName": "check_in",
            "order": "descending"
          }
        ]
      },
      "id": "fetch-reservations",
      "name": "Fetch Reservations",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [900, 180],
      "credentials": {
        "supabaseApi": {
          "id": "YOUR_SUPABASE_CREDENTIAL_ID",
          "name": "Supabase account"
        }
      }
    },
    {
      "parameters": {
        "resource": "row",
        "operation": "getAll",
        "tableId": "availability",
        "returnAll": false,
        "limit": 10000,
        "filterType": "manual",
        "matchMode": "all",
        "filters": {
          "conditions": [
            {
              "keyName": "unit_id",
              "condition": "in",
              "keyValue": "={{ $node[\"Extract IDs\"].json.unit_ids }}"
            },
            {
              "keyName": "date",
              "condition": "gte",
              "keyValue": "={{ new Date().toISOString().split('T')[0] }}"
            },
            {
              "keyName": "date",
              "condition": "lte",
              "keyValue": "={{ new Date(Date.now() + 90*24*60*60*1000).toISOString().split('T')[0] }}"
            }
          ]
        },
        "selectFields": "unit_id, date, status"
      },
      "id": "fetch-availability",
      "name": "Fetch Availability",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [900, 300],
      "credentials": {
        "supabaseApi": {
          "id": "YOUR_SUPABASE_CREDENTIAL_ID",
          "name": "Supabase account"
        }
      }
    },
    {
      "parameters": {
        "resource": "row",
        "operation": "getAll",
        "tableId": "prices",
        "returnAll": false,
        "limit": 10000,
        "filterType": "manual",
        "matchMode": "all",
        "filters": {
          "conditions": [
            {
              "keyName": "unit_id",
              "condition": "in",
              "keyValue": "={{ $node[\"Extract IDs\"].json.unit_ids }}"
            },
            {
              "keyName": "date",
              "condition": "gte",
              "keyValue": "={{ new Date().toISOString().split('T')[0] }}"
            },
            {
              "keyName": "date",
              "condition": "lte",
              "keyValue": "={{ new Date(Date.now() + 90*24*60*60*1000).toISOString().split('T')[0] }}"
            }
          ]
        },
        "selectFields": "unit_id, date, cta, ctd, min, price"
      },
      "id": "fetch-prices",
      "name": "Fetch Prices",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [900, 420],
      "credentials": {
        "supabaseApi": {
          "id": "YOUR_SUPABASE_CREDENTIAL_ID",
          "name": "Supabase account"
        }
      }
    },
    {
      "parameters": {
        "resource": "row",
        "operation": "getAll",
        "tableId": "units",
        "returnAll": false,
        "limit": 1000,
        "filterType": "manual",
        "matchMode": "all",
        "filters": {
          "conditions": [
            {
              "keyName": "id",
              "condition": "in",
              "keyValue": "={{ $node[\"Extract IDs\"].json.unit_ids }}"
            }
          ]
        },
        "selectFields": "id, name, property_id, base_price"
      },
      "id": "fetch-units",
      "name": "Fetch Units",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [900, 540],
      "credentials": {
        "supabaseApi": {
          "id": "YOUR_SUPABASE_CREDENTIAL_ID",
          "name": "Supabase account"
        }
      }
    },
    {
      "parameters": {
        "jsCode": "// Collect all data\nconst notifications = $node[\"Extract IDs\"].json.notifications;\nconst reservations = $node[\"Fetch Reservations\"].all().map(n => n.json);\nconst availability = $node[\"Fetch Availability\"].all().map(n => n.json);\nconst prices = $node[\"Fetch Prices\"].all().map(n => n.json);\nconst units = $node[\"Fetch Units\"].all().map(n => n.json);\n\n// Calculate statistics per unit\nconst unitStats = {};\n\nunits.forEach(unit => {\n  const unitReservations = reservations.filter(r => r.unit_id === unit.id);\n  const unitAvailability = availability.filter(a => a.unit_id === unit.id);\n\n  // Lead time calculation\n  const leadTimes = unitReservations\n    .filter(r => r.created_at && r.check_in)\n    .map(r => {\n      const created = new Date(r.created_at);\n      const checkin = new Date(r.check_in);\n      return Math.floor((checkin - created) / (1000 * 60 * 60 * 24));\n    })\n    .filter(lt => lt >= 0 && lt < 365);\n\n  const avgLeadTime = leadTimes.length > 0\n    ? Math.round(leadTimes.reduce((a, b) => a + b, 0) / leadTimes.length)\n    : null;\n\n  // Weekend vs weekday bookings\n  const weekendBookings = unitReservations.filter(r => {\n    const checkin = new Date(r.check_in);\n    const day = checkin.getDay();\n    return day === 5 || day === 6;\n  }).length;\n\n  const weekendPercentage = unitReservations.length > 0\n    ? Math.round((weekendBookings / unitReservations.length) * 100)\n    : null;\n\n  // Occupancy next 30 days\n  const next30Days = unitAvailability.filter(a => {\n    const date = new Date(a.date);\n    const diff = (date - new Date()) / (1000 * 60 * 60 * 24);\n    return diff >= 0 && diff <= 30;\n  });\n\n  const bookedDays = next30Days.filter(a => a.status === 'blocked').length;\n  const occupancyRate = next30Days.length > 0\n    ? Math.round((bookedDays / next30Days.length) * 100)\n    : null;\n\n  // Average stay length\n  const avgNights = unitReservations.length > 0\n    ? Math.round(unitReservations.reduce((sum, r) => sum + (r.nights || 0), 0) / unitReservations.length)\n    : null;\n\n  unitStats[unit.id] = {\n    unit_name: unit.name,\n    total_bookings_6m: unitReservations.length,\n    avg_lead_time_days: avgLeadTime,\n    weekend_booking_percentage: weekendPercentage,\n    current_occupancy_30d: occupancyRate,\n    avg_stay_nights: avgNights\n  };\n});\n\n// Enrich notifications with context\nconst enrichedNotifications = notifications.map(notif => {\n  const stats = unitStats[notif.unit_id] || {};\n\n  // Get surrounding availability (±7 days)\n  const notifDate = new Date(notif.start_date);\n  const before7 = new Date(notifDate);\n  before7.setDate(before7.getDate() - 7);\n  const after7 = new Date(notifDate);\n  after7.setDate(after7.getDate() + 7);\n\n  const surroundingAvail = availability\n    .filter(a => {\n      const aDate = new Date(a.date);\n      return a.unit_id === notif.unit_id && aDate >= before7 && aDate <= after7;\n    })\n    .sort((a, b) => a.date.localeCompare(b.date))\n    .map(a => `${a.date}: ${a.status}`);\n\n  // Current restrictions for this date\n  const currentRestrictions = prices\n    .filter(p =>\n      p.unit_id === notif.unit_id &&\n      p.date >= notif.start_date &&\n      p.date <= notif.end_date\n    );\n\n  return {\n    ...notif,\n    stats,\n    surrounding_availability: surroundingAvail.slice(0, 15),\n    current_cta: currentRestrictions[0]?.cta || 'brak',\n    current_ctd: currentRestrictions[0]?.ctd || 'brak',\n    current_min: currentRestrictions[0]?.min || 'brak'\n  };\n});\n\n// Build Gemini prompt\nconst prompt = `Jesteś ekspertem od revenue management dla wynajmu krótkoterminowego w Polsce.\n\nZADANIE: Zaproponuj optymalne ustawienia CTA (Close To Arrival) i CTD (Close To Departure) dla ${notifications.length} powiadomień o lukach w dostępności.\n\nDEFINICJE:\n- CTA = ile dni przed przyjazdem przestajemy przyjmować rezerwacje (0 = można bookować do ostatniej chwili)\n- CTD = ile dni przed wyjazdem przestajemy przyjmować rezerwacje\n- MIN = minimalna długość pobytu w dniach\n\nCELE:\n1. Wypełnić jednodniowe luki między rezerwacjami (NAJWYŻSZY PRIORYTET)\n2. Zoptymalizować wykorzystanie kalendarza\n3. Nie blokować długich, wartościowych rezerwacji\n4. Uwzględnić wzorce bookingów i sezonowość\n\nDANE O POWIADOMIENIACH:\n${enrichedNotifications.map((n, i) => `\n${i + 1}. ${n.property_name} - ${n.unit_name}\n   Luka: ${n.start_date} do ${n.end_date} (${n.change_type})\n   Obecne: CTA=${n.current_cta}, CTD=${n.current_ctd}, MIN=${n.current_min}\n\n   Statystyki:\n   - Średni lead time: ${n.stats.avg_lead_time_days || 'brak'} dni\n   - Weekend bookings: ${n.stats.weekend_booking_percentage || 'brak'}%\n   - Obłożenie 30d: ${n.stats.current_occupancy_30d || 'brak'}%\n   - Średnia długość: ${n.stats.avg_stay_nights || 'brak'} nocy\n   - Rezerwacji 6m: ${n.stats.total_bookings_6m || 0}\n\n   Otoczenie:\n${n.surrounding_availability.join('\\n')}\n`).join('\\n')}\n\nZASADY OPTYMALIZACJI:\n1. Jednodniowa luka między rezerwacjami:\n   → CTA=7-14, CTD=0, MIN=2 (wypełni lukę + longer stays)\n\n2. Luka 2-3 dni w sezonie:\n   → CTA=3-7, CTD=0, MIN=2-3 (szansa na short break)\n\n3. Luka 4+ dni z niskim lead time (<10):\n   → CTA=0-3, CTD=0, MIN=2 (last-minute)\n\n4. Luka w low season z długim lead time:\n   → CTA=14-21, CTD=0, MIN=3-5 (dłuższe pobyty)\n\n5. Weekend gap + >70% weekend bookings:\n   → CTA=7-14, MIN=2-3\n\n6. NIE ustawiaj agresywnych restrykcji jeśli:\n   - Occupancy <40%\n   - Lead time >30 dni\n   - Sezon niski (styczeń-marzec, listopad)\n\nOUTPUT: TYLKO VALID JSON, BEZ MARKDOWN\n{\n  \"suggestions\": [\n    {\n      \"notification_id\": \"uuid\",\n      \"unit_id\": \"uuid\",\n      \"unit_name\": \"...\",\n      \"property_name\": \"...\",\n      \"date_start\": \"2026-06-15\",\n      \"date_end\": \"2026-06-16\",\n      \"suggested_cta\": 7,\n      \"suggested_ctd\": 0,\n      \"suggested_min\": 2,\n      \"confidence\": 85,\n      \"reasoning\": \"Jednodniowa luka. CTA=7 da czas na wypełnienie, CTD=0 flexibility, MIN=2 wypełni lukę.\",\n      \"expected_impact\": \"Wysoka szansa na wypełnienie, zachowanie flexibility\"\n    }\n  ]\n}`;\n\nreturn [{\n  json: {\n    prompt,\n    notifications: enrichedNotifications\n  }\n}];"
      },
      "id": "build-prompt",
      "name": "Build Context & Prompt",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1120, 300]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "=https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key={{ $env.GEMINI_API_KEY }}",
        "authentication": "genericCredentialType",
        "genericAuthType": "httpHeaderAuth",
        "sendBody": true,
        "bodyParameters": {
          "parameters": [
            {
              "name": "contents",
              "value": "=[{\"parts\":[{\"text\":\"{{ $json.prompt }}\"}]}]"
            },
            {
              "name": "generationConfig",
              "value": "={\"temperature\":0.3,\"topK\":40,\"topP\":0.95,\"maxOutputTokens\":8192,\"responseMimeType\":\"application/json\"}"
            }
          ]
        },
        "options": {}
      },
      "id": "call-gemini",
      "name": "Call Gemini API",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1340, 300]
    },
    {
      "parameters": {
        "jsCode": "const geminiResponse = $input.item.json;\nconst notifications = $node[\"Build Context & Prompt\"].json.notifications;\n\n// Extract JSON from Gemini response\nconst text = geminiResponse.candidates[0].content.parts[0].text;\nlet parsed;\n\ntry {\n  parsed = JSON.parse(text);\n} catch (e) {\n  // Fallback: extract JSON from markdown\n  const jsonMatch = text.match(/\\{[\\s\\S]*\\}/);\n  if (jsonMatch) {\n    parsed = JSON.parse(jsonMatch[0]);\n  } else {\n    throw new Error('Failed to parse Gemini response');\n  }\n}\n\n// Enrich suggestions with full notification data\nconst enrichedSuggestions = parsed.suggestions.map(s => {\n  const notif = notifications.find(n => n.id === s.notification_id);\n  return {\n    notification_id: s.notification_id,\n    property_id: notif?.property_id,\n    unit_id: s.unit_id,\n    property_name: s.property_name,\n    unit_name: s.unit_name,\n    date_start: s.date_start,\n    date_end: s.date_end,\n    suggested_cta: s.suggested_cta,\n    suggested_ctd: s.suggested_ctd,\n    suggested_min: s.suggested_min,\n    confidence: s.confidence,\n    reasoning: s.reasoning,\n    expected_impact: s.expected_impact,\n    status: 'pending'\n  };\n});\n\nreturn [{\n  json: {\n    suggestions: enrichedSuggestions\n  }\n}];"
      },
      "id": "parse-response",
      "name": "Parse Gemini Response",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1560, 300]
    },
    {
      "parameters": {
        "resource": "row",
        "operation": "create",
        "tableId": "ai_suggestions",
        "dataMode": "defineBelow",
        "fieldsUi": {
          "fieldValues": [
            {
              "fieldId": "={{ $json.suggestions }}"
            }
          ]
        },
        "options": {}
      },
      "id": "insert-suggestions",
      "name": "Insert to Supabase",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [1780, 300],
      "credentials": {
        "supabaseApi": {
          "id": "YOUR_SUPABASE_CREDENTIAL_ID",
          "name": "Supabase account"
        }
      }
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ { \"success\": true, \"suggestions\": $json, \"count\": $json.length, \"generated_at\": new Date().toISOString() } }}",
        "options": {}
      },
      "id": "webhook-response",
      "name": "Respond to Webhook",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [2000, 300]
    }
  ],
  "connections": {
    "Webhook": {
      "main": [
        [
          {
            "node": "Fetch Notifications",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch Notifications": {
      "main": [
        [
          {
            "node": "Extract IDs",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Extract IDs": {
      "main": [
        [
          {
            "node": "Fetch Reservations",
            "type": "main",
            "index": 0
          },
          {
            "node": "Fetch Availability",
            "type": "main",
            "index": 0
          },
          {
            "node": "Fetch Prices",
            "type": "main",
            "index": 0
          },
          {
            "node": "Fetch Units",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch Reservations": {
      "main": [
        [
          {
            "node": "Build Context & Prompt",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch Availability": {
      "main": [
        [
          {
            "node": "Build Context & Prompt",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch Prices": {
      "main": [
        [
          {
            "node": "Build Context & Prompt",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Fetch Units": {
      "main": [
        [
          {
            "node": "Build Context & Prompt",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Context & Prompt": {
      "main": [
        [
          {
            "node": "Call Gemini API",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Call Gemini API": {
      "main": [
        [
          {
            "node": "Parse Gemini Response",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Parse Gemini Response": {
      "main": [
        [
          {
            "node": "Insert to Supabase",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Insert to Supabase": {
      "main": [
        [
          {
            "node": "Respond to Webhook",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "pinData": {},
  "settings": {
    "executionOrder": "v1"
  },
  "staticData": null,
  "tags": [],
  "triggerCount": 0,
  "updatedAt": "2026-01-27T00:00:00.000Z",
  "versionId": "1"
}

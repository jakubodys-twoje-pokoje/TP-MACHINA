# --- logowanie do panelu (wpisz własne) ---------------------------------
EKSPORTER_USER="admin"
EKSPORTER_PASSWORD=""

# --- baza SQLite - plik powstanie sam przy `npm run db:push` ------------
DATABASE_URL="file:./eksporter3000.db"

# --- dostęp do Hotres API (konto agencyjne user+password) ---------------
HOTRES_API_USER="admin@twojepokoje.com.pl"
HOTRES_API_PASSWORD=""

# --- port serwisu (GUI i API na tym samym) ------------------------------
PORT=4000

# --- pliki importu (opcjonalne) ----------------------------------------
# MAX_UPLOAD_MB=200
# UPLOADS_DIR=/opt/machina/eksporter-3000/server/data/uploads
